<?xml version="1.0" encoding="utf-8"?>
<odoo>
	<data>
		<record id="abstract_loan_payment_form_view" model="ir.ui.view">
			<field name="name">abstract.loan.payment.form</field>
			<field name="model">abstract.loan.payment</field>
			<field name="arch" type="xml">
				<form string="Loan Payment">
					<header>
						<button name="action_draft" states="cancelled"
							string="Set to Draft" type="object" />
						<button name="action_cancel" context="{'regenerate':1}"
							states="matched,posted" string="Cancel" type="object" />
						<button name="action_post" states="draft,matched"
							class="oe_highlight" type="object" string="Post" />
						<field name="state" widget="statusbar"
							statusbar_visible="draft,matched,posted,cancelled" />
					</header>
					<sheet>
						<div class="oe_button_box" name="button_box">
						</div>
						<div class="oe_title">
							<h1>
								<field name="name"
									attrs="{'invisible':[('name','=','Draft Payment')]}" />
							</h1>
						</div>
						<group>
							<field name="payment_type" widget="radio"
								attrs="{'readonly': [('state', '!=', 'draft')]}" />
							<field name="partner_type" nolabel="1" widget="selection"
								string=""
								attrs="{'required': [('payment_type', 'in', ('inbound', 'outbound'))], 'invisible': [('payment_type', 'not in', ('inbound', 'outbound'))], 'readonly': [('state', '!=', 'draft')]}" />
							<field name="partner_id" nolabel="1"
								attrs="{'required': [('payment_type', 'in', ('inbound', 'outbound'))], 'invisible': [('payment_type', 'not in', ('inbound', 'outbound'))], 'readonly': [('state', '!=', 'draft')]}"
								context="{'default_is_company': True, 'default_supplier': payment_type == 'outbound', 'default_customer': payment_type == 'inbound'}" />
							<field name="journal_id" widget="selection"
								attrs="{'readonly': [('state', '!=', 'draft')]}" />

							<field name="hide_payment_method" invisible="1" />
							<field name="payment_method_id" string=" " widget="radio"
								attrs="{'invisible': [('hide_payment_method', '=', True)], 'readonly': [('state', '!=', 'draft')]}" />
							<field name="payment_method_code" invisible="1" />
							<field name="currency_id"
								options="{'no_create': True, 'no_open': True}"
								groups="base.group_multi_currency" />
							<field name="amount"
								attrs="{'readonly': [('state', '!=', 'draft')]}" />

						</group>
						<group>
							<field name="payment_date"
								attrs="{'readonly': [('state', '!=', 'draft')]}" />
							<field name="communication"
								attrs="{'invisible': [('state', '!=', 'draft'), ('communication', '=', False)], 'readonly': [('state', '!=', 'draft')]}" />
							<field name="move_id"
								attrs="{'invisible':[('move_id','=',False)]}" />
						</group>
						<notebook>
						</notebook>
					</sheet>
					<div class="oe_chatter">
						<field name="message_follower_ids" widget="mail_followers" />
						<field name="message_ids" widget="mail_thread" />
					</div>
				</form>
			</field>
		</record>

	</data>
</odoo>